pipeline {
     agent any
     stages {
        stage('Lint Frontend Application') {
        }

        stage('Lint Dockerfile') {
        }

        stage('Build docker image') {
        }

        stage('Push docker image') {
        }

        stage('set current kubectl context') {
        }

        stage('Remove docker image') {
        }

        stage('Deploy container to AWS') {
        }
     }
}